{
  "name": "tcp-echo-server",
  "version": "1.0.0",
  "description": "Servidor TCP echo minúsculo para testes e diagnóstico de rede",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["tcp", "echo", "server", "network", "test"],
  "author": "Tox",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
